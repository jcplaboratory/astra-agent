[project]
name = "astra-tui"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["httpx>=0.28", "pydantic-settings>=2.8", "pyjwt>=2.10", "textual>=3.0"]

[project.scripts]
astra-tui = "astra_tui.app:run"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
