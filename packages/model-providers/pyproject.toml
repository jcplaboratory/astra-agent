[project]
name = "astra-model-providers"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["httpx>=0.28", "pydantic>=2.11"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
