[project]
name = "astra-memory"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["astra-domain", "astra-model-providers", "httpx>=0.28"]

[tool.uv.sources]
astra-domain = { workspace = true }
astra-model-providers = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
